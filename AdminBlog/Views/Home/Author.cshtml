@model IEnumerable<AdminBlog.Models.Author>  
@*model olarak bir katregory listesi ekledik burda*@
@{
  ViewData["Title"] = "Author";
}

<button type="button" onclick="$('#AuthorModal').modal('show')" class="btn btn btn-primary">Kategori Ekle</button>

<table class="table table-hover">
  <thead>
    <th scope="col">Id</th>
    <th scope="col">Name</th>
    <th scope="col">Surname</th>
    <th scope="col">Email</th>
    <th scope="col">Parola</th>
    <th scope="col"></th>

  </thead>
  <tbody>
    @foreach (var item in Model)
    {
      <tr>
        <th scope="row">@item.Id</th>
        <td>@item.Name</td>
        <td>@item.Surname</td>
        <td>@item.Email</td>
        <td>@item.Password</td>
        <td><a class="btn btn-danger" asp-route-id="@item.Id" asp-action="DeleteAuthor">Sil</a>
          <button class="btn btn-secondary" onclick="updateForm(@item.Id)" type="button">Güncelle</button>
        </td>
      </tr>
    }
  </tbody>
</table>

<div class="modal" id="AuthorModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Yazar :</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form method="POST" action="/Home/AddAuthor" autocomplete="off">
        <div class="modal-body">
          <div class="form-group">
            <input type="hidden" name="Id" id="Id" class="form-control">
            <input type="text" name="Name" id="Name" class="form-control" placeholder="Ad"> @* name kısımdaki isimler aynı olmak zorunda modeldekiler ile!!! ve homecontrollerdekiler ile !!!
            şu an*@

          </div>
          <div class="form-group">
            <input type="text" name="Surname" id="Surname" class="form-control" placeholder="Soyad"> @* name kısımdaki isimler aynı olmak zorunda
            şu an*@
          </div>

         <div class="form-group">
            <input type="text" name="Email" id="Email" class="form-control" placeholder="Email"> @* name kısımdaki isimler aynı olmak zorunda
            şu an*@
          </div>

          <div class="form-group">
            <input type="text" name="Password" id="Password" class="form-control" placeholder="Şifre"> @* name kısımdaki isimler aynı olmak zorunda
            şu an*@
          </div>

        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" onclick="clearForm()" data-bs-dismiss="modal">Kapat</button>
          <button type="submit" id="saveBtn" class="btn btn-primary">Ekle</button>
          <button type="submit" id="updateBtn" class="btn btn-primary">güncelle</button>

        </div>
      </form>
    </div>
  </div>
</div>

@section Scripts {
  <script>
    $(() => clearFrom())
    function updateForm(Id) {
      fetch('/Home/AuthorDetails?Id=' + Id)
        .then(res => {
          if (!res.ok) { // Eğer HTTP cevabı 200 değilse hata fırlat
            throw new Error('Kategori bulunamadı veya ağ sorunu: ' + res.statusText);
          }
          return res.json(); // Eğer başarılıysa JSON formatına çevir
        })
        .then(res => {
          $('#Id').val(res.id);  // Id alanını doldur
          $('#Name').val(res.name);  // Name alanını doldur
          $('#Surname').val(res.surname);  // Id alanını doldur
          $('#Email').val(res.email);
          $('#Password').val(res.password);
        })
        .catch(error => {
          console.error('Error fetching Author details:', error); // Konsola hata mesajı yazdır
          alert("Kategori bilgileri alınamadı, lütfen tekrar deneyin."); // Kullanıcıya hata mesajı göster
        });

      $('#saveBtn').hide();  // Ekle butonunu gizle
      $('#updateBtn').show();  // Güncelle butonunu göster
      $('#AuthorModal').modal('show');  // Modalı aç
    }
    function clearFrom() {
      $('#Id').val('')
      $('#Name').val('')
      $('#Surname').val('');  // Id alanını doldur
      $('#Email').val('');
      $('#Password').val('');
      $('#saveBtn').show();
      $('#updateBtn').hide();
    }
  </script>
}@*update fonksiyonuun buradaya script olarak ekledik*@